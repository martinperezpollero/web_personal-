---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { ui } from '../../i18n/ui';
import publications from '../../data/publications.json';

// CSL-JSON exported from Zotero (CLAUDE.md §7). Not edited by hand.
const items = publications as Array<Record<string, any>>;
---

<BaseLayout
  title="publications — Martín Pérez Pollero"
  description="Selected publications by Martín Pérez Pollero."
>
  <h1>publications</h1>

  {
    items.length === 0 ? (
      <p><em>{ui.en['publications.empty']}</em></p>
    ) : (
      <ul>
        {items.map((it) => (
          <li>
            <span class="pub-title">{it.title}</span>
            {it.issued?.['date-parts']?.[0]?.[0] && <span> ({it.issued['date-parts'][0][0]})</span>}
            {it.DOI && <> · <a href={`https://doi.org/${it.DOI}`}>DOI</a></>}
            {it.URL && <> · <a href={it.URL}>full text</a></>}
          </li>
        ))}
      </ul>
    )
  }
</BaseLayout>
